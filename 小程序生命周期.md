<!-- vue的 -->
<script setup lang="ts">
import {
  onBeforeMount,  // 组件挂载前
  onMounted,      // 组件挂载完成
  onBeforeUpdate, // 组件更新前
  onUpdated,      // 组件更新完成
  onBeforeUnmount, // 组件卸载前
  onUnmounted,    // 组件卸载完成
  onActivated,    // 被 keep-alive 缓存的组件激活
  onDeactivated,  // 被 keep-alive 缓存的组件停用
  onErrorCaptured // 捕获子孙组件错误
} from 'vue'

// 1. 组件挂载前
onBeforeMount(() => {
  console.log('组件挂载前')
})

// 2. 组件挂载完成
onMounted(() => {
  console.log('组件挂载完成')
  // 可以在这里进行数据初始化、API调用等
})

// 3. 组件更新前
onBeforeUpdate(() => {
  console.log('组件更新前')
})

// 4. 组件更新完成
onUpdated(() => {
  console.log('组件更新完成')
})

// 5. 组件卸载前
onBeforeUnmount(() => {
  console.log('组件卸载前')
})

// 6. 组件卸载完成
onUnmounted(() => {
  console.log('组件卸载完成')
  // 清理定时器、取消事件监听等
})

// 7. 被 keep-alive 缓存的组件激活
onActivated(() => {
  console.log('组件激活')
})

// 8. 被 keep-alive 缓存的组件停用
onDeactivated(() => {
  console.log('组件停用')
})

// 9. 错误捕获
onErrorCaptured((err, instance, info) => {
  console.error('捕获到组件错误:', err)
  return false // 阻止错误继续向上传播
})
</script>
<!-- uniapp独有的 -->
<script setup lang="ts">
import {
  onLoad,           // 页面加载
  onShow,           // 页面显示
  onReady,          // 页面初次渲染完成
  onHide,           // 页面隐藏
  onUnload,         // 页面卸载
  onPullDownRefresh, // 下拉刷新
  onReachBottom,    // 页面上拉触底
  onPageScroll,     // 页面滚动
  onTabItemTap,     // tab 点击
  onShareAppMessage, // 分享
  onShareTimeline,  // 分享到朋友圈
  onAddToFavorites, // 添加到收藏
  onBackPress,      // 返回按钮
  onNavigationBarButtonTap, // 导航栏按钮点击
  onResize,         // 窗口尺寸变化
} from '@dcloudio/uni-app'

// 1. 页面加载 - 参数为上个页面传递的数据
onLoad((options) => {
  console.log('页面加载，参数:', options)
  // 可以在这里接收页面参数
  const { id, name } = options
})

// 2. 页面显示 - 每次页面显示都会触发
onShow(() => {
  console.log('页面显示')
  // 适合做页面数据刷新
})

// 3. 页面初次渲染完成 - 只会触发一次
onReady(() => {
  console.log('页面初次渲染完成')
  // 适合操作DOM
})

// 4. 页面隐藏
onHide(() => {
  console.log('页面隐藏')
})

// 5. 页面卸载
onUnload(() => {
  console.log('页面卸载')
})

// 6. 下拉刷新
onPullDownRefresh(() => {
  console.log('下拉刷新')
  
  // 模拟异步操作
  setTimeout(() => {
    // 停止下拉刷新动画
    uni.stopPullDownRefresh()
  }, 1000)
})

// 7. 页面上拉触底
onReachBottom(() => {
  console.log('页面上拉触底')
  // 加载更多数据
  loadMoreData()
})

// 8. 页面滚动
onPageScroll((e) => {
  console.log('页面滚动，滚动位置:', e.scrollTop)
  // 可以在这里实现滚动吸顶等效果
})

// 9. tab 点击（仅tabbar页面有效）
onTabItemTap((e) => {
  console.log('tab点击:', e)
  // e.index - 被点击tabItem的索引
  // e.pagePath - 被点击tabItem的页面路径
  // e.text - 被点击tabItem的按钮文字
})

// 10. 用户点击右上角分享（小程序）
onShareAppMessage((res) => {
  console.log('分享:', res)
  return {
    title: '分享标题',
    path: '/pages/index/index',
    imageUrl: '/static/share.jpg'
  }
})

// 11. 分享到朋友圈（微信小程序）
onShareTimeline(() => {
  return {
    title: '分享到朋友圈',
    query: 'id=123'
  }
})

// 12. 添加到收藏（微信小程序）
onAddToFavorites((res) => {
  return {
    title: '收藏标题',
    imageUrl: '/static/favicon.jpg',
    query: 'id=123'
  }
})

// 13. 返回按钮/手势返回
onBackPress((e) => {
  console.log('返回按钮事件:', e)
  // e.from: backbutton(返回按钮) | navigateBack(uni.navigateBack)
  
  // 返回 true 表示阻止默认返回行为
  // 可以在这里实现"再按一次退出"功能
  return false // 允许返回
})

// 14. 导航栏按钮点击
onNavigationBarButtonTap((e) => {
  console.log('导航栏按钮点击:', e)
  // e.index - 按钮索引，0表示左边第一个
  // e.text - 按钮文字（如果有）
})

// 15. 窗口尺寸变化（仅小程序端支持）
onResize((size) => {
  console.log('窗口尺寸变化:', size)
  // size.windowWidth - 窗口宽度
  // size.windowHeight - 窗口高度
})

const loadMoreData = () => {
  // 加载更多数据的逻辑
}
</script>
完整的生命周期执行顺序
进入页面：
1. onLoad(options)           ← 最先执行，接收页面参数
2. onShow()                 ← 页面显示
3. onBeforeMount()          ← Vue组件挂载前
4. onMounted()              ← Vue组件挂载完成
5. onReady()                ← 页面初次渲染完成

离开页面（通过navigateTo跳转）：
1. onHide()                 ← 页面隐藏
2. onBeforeUnmount()        ← Vue组件卸载前
3. onUnmounted()            ← Vue组件卸载完成
4. onUnload()               ← 页面卸载

返回页面（通过navigateBack返回）：
1. onShow()                 ← 页面重新显示
2. onActivated()            ← 如果使用keep-alive缓存

下拉刷新：
1. onPullDownRefresh()      ← 触发下拉刷新

上拉触底：
1. onReachBottom()          ← 页面滚动到底部
![alt text](image-1.png)